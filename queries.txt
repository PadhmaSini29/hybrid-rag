python app.py ask "What is machine learning?"
python app.py ask "What are the main types of machine learning systems?"
python app.py ask "What is supervised learning?"
python app.py ask "What is unsupervised learning?"
python app.py ask "What is the difference between classification and regression?"
python app.py ask "Why is machine learning useful compared to traditional programming?"
python app.py ask "What are features and labels in machine learning?"
python app.py ask "What is overfitting in machine learning?"

python app.py ask "What are the main challenges of machine learning systems?"
python app.py ask "Explain batch learning vs online learning."
python app.py ask "What is the difference between instance-based and model-based learning?"
python app.py ask "What are the steps in an end-to-end machine learning project?"
python app.py ask "What is cross-validation and why is it used?"
python app.py ask "Explain precision and recall and their trade-off."
python app.py ask "What is the role of feature scaling in machine learning?"
python app.py ask "How does gradient descent work in training models?"
python app.py ask "What are ensemble methods and why are they effective?"

python app.py ask "What is Text-to-SQL?"
python app.py ask "What is the main goal of this paper?"
python app.py ask "What is DAIL-SQL?"
python app.py ask "What datasets are used in the paper?"
python app.py ask "What accuracy does DAIL-SQL achieve?"
python app.py ask "What are Large Language Models (LLMs)?"

python app.py ask "What are the key components of prompt engineering in Text-to-SQL?"
python app.py ask "What is the difference between zero-shot and few-shot learning?"
python app.py ask "What is in-context learning and how is it used here?"
python app.py ask "What are the different question representation methods?"
python app.py ask "What are the example selection strategies?"
python app.py ask "What evaluation metrics are used in this paper?"
